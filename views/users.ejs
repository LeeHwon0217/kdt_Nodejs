<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>사용자 목록</title>
  </head>
  <body>
    <ul>
      <% users.forEach(function(user) { %>
      <li><%=user%></li>
      <% }) %>
    </ul>
  </body>
</html>
